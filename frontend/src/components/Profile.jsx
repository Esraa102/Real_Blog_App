import { UpdateProfileForm, UploadImgProfile } from ".";

const Profile = () => {
  return (
    <div>
      <UploadImgProfile />
      <UpdateProfileForm />
    </div>
  );
};

export default Profile;
